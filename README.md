### Bruteforce-Centreon-IT-and-Networking-monitoring-tool
```
usage: bruteforcing_centreon_v1904.py -u url -pf password_file

optional arguments:
  -h, --help            show this help message and exit
  -u URL, --url URL     url
  -o OUTPUTFILE, --outputfile OUTPUTFILE
                        output file
  -pf PASSWORD_FILE, --password_file PASSWORD_FILE
                        password file
  -t THREADS, --threads THREADS
                        threads

```
